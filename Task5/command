# Assuming the command is run on the server container, use the following curl command to send a POST call.
curl -X POST \
-H "Content-Type: application/json" \
-d '{"name":"test_store"}' \
http://localhost:8000/stores